<UserControl x:Class="SimpleDataGrid.ReadOnlyGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:SimpleDataGrid="clr-namespace:SimpleDataGrid" mc:Ignorable="d"
             d:DataContext="{x:Static SimpleDataGrid:DesignTimeTestData.ViewModel}"
             d:DesignHeight="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <SimpleDataGrid:DataGridExt
            x:Name="dataGrid"
            Grid.Row="0"
            IsReadOnly="True"
            AutoGenerateColumns="False"
            EnableRowVirtualization="False"
            ItemsSource="{Binding Entities}"
            SelectionMode="Single">
        </SimpleDataGrid:DataGridExt>
        <StackPanel x:Name="sp" Grid.Row="1" Orientation="Horizontal">
            <SimpleDataGrid:Pager DataContext="{Binding PagerViewModel}"/>
        </StackPanel>
    </Grid>
</UserControl>
